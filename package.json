{
  "name": "attendance-app-root",
  "version": "1.0.0",
  "description": "Root package for the attendance app",
  "scripts": {
    "build": "cd app && npm install --production=false && npm run build:client && npm run build:server",
    "start": "cd app && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
